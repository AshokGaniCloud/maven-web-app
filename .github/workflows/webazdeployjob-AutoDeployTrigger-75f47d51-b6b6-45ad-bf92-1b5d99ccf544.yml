name: Trigger auto deployment for webazdeployjob

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/webazdeployjob-AutoDeployTrigger-75f47d51-b6b6-45ad-bf92-1b5d99ccf544.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.WEBAZDEPLOYJOB_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.WEBAZDEPLOYJOB_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.WEBAZDEPLOYJOB_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.WEBAZDEPLOYJOB_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.WEBAZDEPLOYJOB_REGISTRY_PASSWORD }}
          containerAppName: webazdeployjob
          resourceGroup: webazcon
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


